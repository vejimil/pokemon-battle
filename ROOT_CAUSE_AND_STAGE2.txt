Current progress summary

Asset-path fix
- src/app.js still auto-detects the working sprite root and item root at startup.
- Sprite and item icon requests use the detected base path instead of one hardcoded path.

Stage 2 progress now implemented
- The app now attempts to load a Showdown-compatible Dex runtime in the browser before falling back to PokéAPI.
- Species, move, item, and ability lookup can now come from that Dex layer.
- Saved teams are rehydrated against the loaded data runtime on startup.
- Validation now checks native abilities, valid items, duplicate moves, and whether selected moves appear in the species learnset provided by the loaded Dex.

Next milestone
- Replace the remaining custom battle resolution with a full simulator layer.
- Add real format-based legality using TeamValidator or an equivalent Showdown-compatible validator.
