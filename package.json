{
  "name": "pkb-showdown-local-migration",
  "private": true,
  "version": "0.18.0-stage18-engine-first-runtime-mode",
  "description": "PKB engine-first migration step with explicit runtime modes and guarded legacy fallback",
  "scripts": {
    "start": "node ./server/server.cjs"
  }
}
